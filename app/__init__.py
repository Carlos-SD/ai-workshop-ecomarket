"""EcoMarket AI Agent package.

This package contains the agent and tool implementations for the Final Project.
The legacy CLI scripts in ../scripts/ (order_query.py, return_query.py) remain
for backwards compatibility but are superseded by app.agent.
"""
